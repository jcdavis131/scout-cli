# Openswap — paid-SaaS replacement adapters

One adapter per paid platform, built on the Harper pattern: name the enemy,
make privacy architectural (no server, zero network calls — the manifest
denies the network axis, so the guarantee is falsifiable), let a local core do
real work day one, and detect-and-use the best native open binary when it
appears on PATH.

## Family contract (every adapter)

- **[A] detect_local_capability** — probe PATH for the native binary, confirm
  health with a cheap read-only subcommand, report
  `{tier: native|fallback|unavailable, native: {binary, found, path, version,
  probe_cmd, probe_output}}`.
- **[B] best_available_tier** — native binary when present → pure-stdlib
  fallback core (must self-describe its reduced scope) → explicit
  "unavailable" with an install hint. NEVER a silent no-op, NEVER a
  network/SaaS fallback tier.
- **[C] normalized diagnostics** — every tier emits
  `{path, line, col, rule, severity, message, suggestion, source}` with
  severity ∈ error|warning|suggestion|info.

**Shared base:** `bigbang/core/openswap.py` (`probe_binary`,
`capability_report`, `diagnostic`, `sort_diagnostics`, `summarize`,
`severity_rank`). Later adapters REUSE it — do not re-implement detection or
the schema. Each adapter follows the standard plugin recipe (see
`docs/EXTENDING.md`): pure logic in `bigbang/core/<name>.py`, real I/O in
`bigbang/plugins/<name>/cli.py`, default-deny manifest, then add a row here.

## Adapters

| # | Paid platform | Open replacement | Scout surface | Tier today |
|---|---------------|------------------|---------------|------------|
| 1 | Grammarly Premium | harper (Automattic, Rust) native tier + pure-stdlib heuristic prose linter | `scout prose` — `lint`, `detect`, `rules`, `hello`; `--fail-on` is the pre-publish gate hook | fallback — harper-cli verified ABSENT on PATH 2026-07-23; the stdlib core carries launch |
| 2 | UptimeRobot / Pingdom | Uptime Kuma pattern: stdlib http.client/ssl probe loop, flap-damped incident state machine, sqlite3 ledger (`.scout/uptime.db` — shared substrate for heartbeat #6, cert monitor #9, status page, alert router) | `scout uptime` — `check`, `watch`, `status`, `history`, `targets`, `mark`, `detect`, `hello`; `--fail-down` is the cron/CI gate hook | fallback by design — Uptime Kuma ships as a node server, no CLI binary exists to prefer; the stdlib core is 100% of the product |
| 3 | Screaming Frog + Semrush + Yoast (merged category) | polite same-host crawler (urllib + html.parser + urllib.robotparser, resumable sqlite3 frontier in `.scout/seo.db`) + modular on-page audit: status codes, title/description windows, canonicals, single-h1, noindex, redirect chains, OG/Twitter completeness, alt coverage, JSON-LD parseability, hashlib duplicate titles, SF-shaped per-URL CSV (crawl store is the substrate for broken-links #4, sitemap #10, a11y #25, secure-headers #22) | `scout seo` — `crawl`, `audit`, `lint`, `sites`, `detect`, `hello`; `--fail-on` is the pre-deploy gate hook | fallback by design — no open CLI binary exists in this category (SEOnaut is a web server; Screaming Frog is the paid enemy); the stdlib core is 100% of the product |
| 4 | Ahrefs (broken-link) / Dr. Link Check | linkchecker pattern riding the #3 crawl store: internal links verified against CRAWL RESULTS (zero refetches), dict-adjacency link graph with orphan/link-to-redirect reports, opt-in HEAD-then-GET external verification (per-domain rate limiting, retry/backoff, wall-clock budget, every URL policy-gated first — a denied URL records as unverified, never fetched), offline README/docs cross-link + anchor-fragment checking (html.parser ids + GitHub-style heading slugs), diffable sqlite3 run ledger in `.scout/links.db`, config `external_allow` trust list | `scout links` — `check`, `files`, `graph`, `diff`, `detect`, `hello`; `check --fail-on` is the weekly-cron/per-deploy gate, `diff --fail-on-new` the CI regression gate | fallback by design — linkchecker/lychee are probed and surfaced for manual use but never executed (a spawned binary fetches outside the per-URL policy gate); the stdlib core is 100% of the product |
| 5 | Checkly (synthetic monitoring) | post-deploy smoke harness: per-site JSON manifest of URL→expected-string checks (`{"<url>": "<expected>"}` or named `{url, expect, max_ms}`), urllib polling (redirects followed — asserts on what a visitor receives) under a bounded retry/backoff budget, nonzero exit with a per-URL pass/fail report; `--mark` drops deploy + outcome events into the #2 uptime ledger so incidents correlate with deploys | `scout smoke` — `run`, `plan`, `detect`, `hello`; `run` (exit-1-on-fail is the default) is the after-every-`vercel deploy` gate hook | fallback by design — Checkly's CLI is the paid enemy's SaaS client (a forbidden network tier); hurl is probed/surfaced for manual use but never executed (a spawned binary fetches outside the per-URL policy gate); the stdlib core is 100% of the product |
| 6 | Healthchecks.io / Cronitor | dead-man's-switch heartbeat registry on the #2 uptime ledger (same sqlite file, `beats` table + `hb:`-namespaced state/incidents/events — no parallel store): one-line beats (`scout heartbeat beat trainer`, a touched file's mtime, or GET /beat/&lt;daemon&gt; on an optional LOOPBACK-only http.server), per-daemon grace periods + expected-cadence profiles as JSON config (defaults: trainer, research-loop, factory-checkpointer, watch-rearm — the org's "trainer done vs crash" and "watches die with sessions" failure modes), stale watcher writing alert/recovery events for the alert-router | `scout heartbeat` — `beat`, `sweep`, `watch`, `status`, `daemons`, `serve`, `detect`, `hello`; `sweep --fail-stale` is the cron/CI gate hook | fallback by design — Healthchecks' open-source release is a Django web app, no CLI binary exists to prefer; runitor (a healthchecks.io SaaS pinger) is probed/surfaced but never executed (its whole job is a network ping to the paid enemy — the forbidden tier); the stdlib core is 100% of the product |
| 7 | GitGuardian / TruffleHog Enterprise | detect-secrets/gitleaks pattern, 100% stdlib: JSON-configurable regex signature pack (AWS/GitHub/GitLab/Slack/Stripe/Google/OpenAI/Anthropic/npm/SendGrid shapes, PEM headers, JWTs) + Shannon-entropy scoring (re + math) over os.walk trees and read-only `git diff --cached` / `git log -p` patch text; findings always redacted (first 4 chars + length — the report itself is safe to share), sha256 fingerprints for stable allowlisting (hash rule:secret, so they survive line moves), span-overlap suppression (one secret = one finding under the most specific rule), per-file-type entropy thresholds, four allowlist escape hatches (rules/paths/patterns/fingerprints), signature packs + disables as config | `scout leaks` — `scan`, `staged`, `history`, `signatures`, `detect`, `hello`; `staged --fail-on warning` is the pre-commit hook, `scan --fail-on error` the security-audit sweep gate | fallback by design — gitleaks/trufflehog are probed and surfaced for manual cross-checks but never executed (a gate whose verdict changes with PATH contents is flaky by construction); the stdlib core is 100% of the product |
| 8 | Sentry | GlitchTip-lite, 100% stdlib: capture via logging.Handler (UNFORMATTED record.msg template as the grouping key) + chaining sys.excepthook (KeyboardInterrupt passes through; capture failure never masks the crash) + crash-log ingest (parses the LAST complete traceback block out of arbitrary log text — retro-instruments daemons never touched); sha256 fingerprint over exception type + per-frame (path tail, function) — line numbers and messages excluded so edits/interpolations never shard an issue; sqlite issue store (`.scout/glitch.db` — own file, never contends with the #2 ledger's write lock) with first/last-seen, counts, open/resolved/ignored triage (resolved reopens as a regression, ignored stays ignored), per-occurrence JSON context, per-project retention as JSON config (counters survive pruning); static self-contained HTML issue browser (html.escape everywhere, zero JS, works from file://) | `scout glitch` — `log`, `ingest`, `issues`, `show`, `mark`, `report`, `prune`, `detect`, `hello`; `issues --fail-on error` is the cron/CI gate hook; in-process capture via `from bigbang.core import glitch; glitch.install(db, project=...)` | fallback by design — GlitchTip's open-source release is a Django web app, no CLI binary exists to prefer; sentry-cli is probed/surfaced but never executed (its whole job is uploading stack traces to the paid SaaS — the forbidden network tier); the stdlib core is 100% of the product |
| 9 | SSL Labs (Server Test) / TrackSSL | local TLS monitor: the handshake runs on THIS box (ssl.create_default_context + getpeercert under a timeout — the only real I/O), everything deterministic in stdlib — locale-independent notAfter parse (hardcoded English months + calendar.timegm, never locale-sensitive strptime), days-to-expiry, RFC-6125 left-label wildcard SAN/CN host match (CN honored only when no SAN), issuer-vs-subject self-signed + chain-presence detection, weak-protocol flags (SSLv2/3, TLSv1/1.1), HSTS-header check; classify expired/expiring-soon(<21d)/host-mismatch/self-signed/weak-protocol → error, expiring(<45d)/missing-HSTS/no-chain → warning (windows are config); rides the #2 uptime ledger via one idempotent `certs` table (CREATE TABLE IF NOT EXISTS — never alters uptime's tables) and records non-ok findings as kind="cert" events on the SAME monitoring timeline | `scout certmon` — `check`, `status`, `detect`, `hello`; default targets are the https hosts of uptime.DEFAULT_TARGETS (derived, not duplicated); `check --fail-on {error,warning}` is the pre-expiry cron/CI gate hook | fallback by design — SSL Labs' Server Test and TrackSSL are both SaaS, no local native CLI is a superset of this core; openssl is surfaced as an optional local helper and ssllabs-scan as a SaaS client that is named but never executed (the forbidden network tier); the stdlib ssl core is 100% of the product |
| 10 | Weights & Biases | local experiment tracker, 100% stdlib: own sqlite store (`.scout/runtrack.db` — own file, never contends with the #2 ledger's write lock) of runs (name, JSON config/provenance, running→finished\|failed status, created/finished ts) + per-step scalar metrics (auto-incrementing step like wandb.log, or an explicit step axis); start_run/log_metrics (validate-before-write, all-or-nothing; rejects bool/NaN/inf/non-numeric)/finish_run, run_history (plot-ready ascending), run_summary (first/last/min/max per key), compare_runs (last/min/max per run for every key + deltas vs a baseline run); no server, no DSN, no sync — the file IS the tracker; `list --fail-on warning` maps a failed run onto the family diagnostic schema, but the tracker itself is the value | `scout runtrack` — `start`, `log`, `finish`, `list`, `show`, `compare`, `detect`, `hello`; `list --fail-on warning` is the CI gate on a crashed experiment | fallback by design — W&B's product is the hosted service; mlflow is surfaced as an optional local alternative and the wandb CLI as a SaaS client that is named but never executed (its whole job is syncing runs to the paid SaaS — the forbidden network tier); the manifest disables the network axis entirely, so "no run ever left the box" is architectural; the stdlib sqlite tracker is 100% of the product |
| 18 | StatusPage.io / Atlassian Statuspage | static status page generator that COLLECTS NOTHING: read-only over the ledgers #2/#9/#6 already wrote (`open_readonly` uses sqlite `mode=ro`, so writes to the shared `.scout/uptime.db` physically fail and the page never contends for uptime's write lock), one self-contained HTML file (inline CSS, zero JavaScript, zero external assets, `html.escape` everywhere — works from `file://`); per-service confirmed state + windowed availability/latency percentiles + state mix (degraded stays visible behind the percentage) + last incident open-or-closed, cert expiry posture, heartbeat liveness, the shared deploy/alert/cert timeline, and a provenance table naming every source table and row count. Honesty is the product: services are derived from what was RECORDED (`SELECT DISTINCT target FROM checks`), never from configured targets; a source is `present` only when its table exists AND holds a row; an absent ledger renders an explicit no-data state with NO percentage anywhere instead of a green board; stale rows (newest check older than `--stale-after`) are flagged and drag the roll-up to degraded; heartbeat grace periods are never guessed | `scout statuspage` — `sources`, `status`, `render`, `detect`, `hello`; `--fail-on {error,warning}` is the cron/CI gate and fires on outages AND on stale/absent data (a page that went blind is a real incident) | fallback by design — Atlassian Statuspage is SaaS, cState ships as a Hugo theme with no CLI binary, and statping-ng is a server that does its own collecting (the opposite of this design); hugo and statping are probed and surfaced but never executed; the manifest disables the network axis entirely — no probe to allowlist, no SaaS to feed — so "nothing on this page ever left the box" is architectural; the stdlib renderer is 100% of the product |
| 12 | Feedly Pro | miniflux pattern, 100% stdlib: xml.etree parser for RSS 2.0 / RSS 1.0 (RDF) / Atom behind ONE namespace shim (`local()` folds `{ns}tag` to a lowercase local name, so dc:creator, content:encoded and Atom's rel="alternate" links all resolve without a per-format namespace map), locale-independent timestamps (email.utils RFC-822 + fromisoformat ISO-8601 with explicit `Z` normalization — never strptime's `%b`), conditional GET from ETag/Last-Modified stored on the feed row (a 200 with no ETag CLEARS the stale one, or a changing feed would answer 304 forever), sha256 dedupe by entry id → link → title+date with `UNIQUE(feed, key)` + INSERT OR IGNORE as the dedupe itself, optional keyword scoring (title hits outweigh body hits, each keyword counts once at its best field, phrases match across wrapped whitespace; weights are JSON config), own sqlite store (`.scout/feeds.db` — never contends with the #2 ledger's write lock), digest emitter in both shapes (ranked JSON items + a deterministic UTC text render) with `--new`/`--mark` so a scheduled digest never repeats an item; a DOCTYPE is refused before parsing, which closes the entity-expansion hole without taking a defusedxml dependency | `scout feeds` — `add`, `list`, `fetch`, `digest`, `detect`, `hello`; `add --seed` registers the research feeds, `fetch --fail-on error` is the cron/CI gate against silent research ingestion, `digest --min-score N --new --mark` is the daily read | fallback by design — miniflux and FreshRSS are servers and newsboat is a TUI, none a superset of "dedupe, score, emit a JSON digest"; newsboat/rsstail are probed and surfaced but never executed (a spawned reader fetches outside the per-URL policy gate — the links #4 doctrine); seed feeds are gated by the manifest allowlist and hand-added feeds by the persisted user allowlist, and a URL in neither records state "denied" with no socket opened; the stdlib core is 100% of the product |
| 20 | Elastic Cloud / Algolia | local full-text search, 100% stdlib: one sqlite3 FTS5 virtual table IS the engine (`.scout/search.db` — own file, never contends with the #2 ledger's write lock), `docs(path, body)` sharing `files.id` as its rowid so a hit joins straight back to stat/sha256 provenance; FTS5 is a compile-time option so it is PROBED end to end (virtual table + `unicode61 remove_diacritics 2` + `bm25()` + `snippet()`, in-memory) and `open_index` raises rather than silently indexing nothing; deterministic corpus discovery (sorted walk, case-insensitive include/exclude globs, vendor/VCS prune list derived from leaks #7, extension defaults derived from prose #1 + seo #3, explicit skip reasons for too-large/binary/unreadable, utf-8-sig so a PowerShell BOM never lands in a snippet); incremental reindex by mtime+size (fast, and honest that it misses mtime-preserving edits) or by content sha256 (`--mode hash`, which catches them); BM25 ranking with per-column weights (path 2x body by default), `snippet()` highlighting with configurable markers, wildcard-free `--path <subtree>` filtering applied inside SQL so `total` matches the page, pagination; prune scoped to the roots being indexed AND to roots that still exist (an unmounted path is not evidence its files are gone); stat-only staleness audit mapping missing/stale/skipped onto the family diagnostic schema | `scout search` — `index`, `query`, `stats`, `detect`, `hello`; `stats --fail-on warning` is the reindex-before-you-trust-it gate, `query --fail-empty` the CI "the docs must mention X" assertion, `index --fail-on info` the "nothing was silently skipped" gate | fallback by design — Elastic Cloud's product is the managed cluster and Meilisearch/Quickwit/Typesense ship as servers; recollindex is surfaced as an optional local indexer and rg as an unindexed complement, while ecctl (Elastic Cloud's control CLI) is named but NEVER executed — its whole job is driving the paid SaaS (the forbidden network tier); the manifest disables the network axis entirely, so "no document ever left the box" is architectural; the stdlib FTS5 core is 100% of the product |
| 14 | Papertrail / Splunk Cloud / Loggly | local log pipeline, 100% stdlib: tailing collectors that keep a per-file byte offset in sqlite (Path.stat/os.stat size + mtime; a shrunken file is a rotation and resets to the top; a trailing partial line stays unconsumed until its newline arrives) and NEVER hold a handle (open->seek->read->close, so a Windows writer can still rename its own log); encoding is SNIFFED not assumed — BOM table with UTF-32-LE tested before the UTF-16-LE prefix it starts with, NUL-position heuristic for BOM-less UTF-16, then strict UTF-8, then latin-1 — because this repo's own research logs (apps/dottie/data/research/logs/run.log) are UTF-16-LE and newlines are matched as ENCODED byte patterns (b"\r\n\x00") on code-unit boundaries so a resumed read never lands mid-character; per-source parsers (iso / bracket / syslog / jsonl / plain + a custom `regex` escape hatch), level normalization over syslog+python+node spellings with a word-boundary sniffer for unlabelled lines, locale-independent timestamps (hardcoded English months + calendar.timegm, never strptime; naive = UTC unless the source declares tz_offset; syslog's missing year comes from the FILE's mtime, never from "now"); indexed sqlite store (`.scout/logs.db` — own file, never contends with the #2 ledger's write lock) with ts/source/level_rank indexes, `dated` recording whether the time came from the line or from ingest; queries filter by time range, MINIMUM level (a floor, not equality), source and message-or-raw substring, plus level/source/time-bucket rollups | `scout logs` — `collect`, `query`, `rollup`, `sources`, `parsers`, `detect`, `hello`; `collect --fail-on error` gates on THIS pass's lines (a stale error must not fail today's cron), `query --fail-on` gates on the stored window | fallback by design — Papertrail, Splunk Cloud and Loggly are hosted ingest services; lnav is surfaced as an optional local TUI viewer (no incremental offset store, so it complements this) and the papertrail/splunk clients as SaaS shippers that are named but NEVER executed (their whole job is uploading log lines — the forbidden network tier); the manifest disables the network axis entirely, so "no log line ever left the box" is architectural; the stdlib pipeline is 100% of the product |
| 22 | Detectify / Burp Suite Pro (surface pane) | local secure-header + exposed-surface auditor, 100% stdlib: one urllib GET on THIS box (redirect chain recorded — the visitor's posture is the FINAL response's) feeding a deterministic core: CSP (enforced vs Report-Only, script-src via the browser's own default-src fallback, 'unsafe-inline'/'unsafe-eval', wildcard and scheme-wide sources, conflicting duplicate policies, frame-ancestors/object-src hygiene), HSTS (max-age parse, max-age=0 as an explicit FORGET, short windows, includeSubDomains, preload), X-Frame-Options incl. the dead ALLOW-FROM form with CSP frame-ancestors accepted as the modern substitute, nosniff, Referrer-Policy, Permissions-Policy, ACAO wildcard (error when combined with Allow-Credentials), Server/X-Powered-By version disclosure, per-cookie Secure/HttpOnly/SameSite plus the __Secure-/__Host- name-prefix contracts (headers are captured as PAIRS, never a dict, so repeated Set-Cookie headers all get graded), directory-listing signatures (Apache/nginx "Index of /", Tomcat, IIS) and active/passive mixed content from html.parser subresources; severity contract is DATA (RULES code -> severity+remedy, `severity` overrides and `ignore_rules` as JSON config), A+..F grade that ignores info-level disclosure notes, and history in ONE additive `header_scans` table on the shared #3 crawl store (`.scout/seo.db` — `audit` re-reads the `pages` rows `seo crawl` already wrote, the extension point the seo core documents, so the fleet sweep costs zero requests). Honesty is enforced: an unreachable URL grades None (never a fabricated F), and the body-dependent checks (mixed content, directory listing) are reported in `checks_skipped` when no body is available instead of passing silently, with the store's collapsed-Set-Cookie undercount surfaced as an explicit caveat | `scout headers` — `scan` (live, `--dirs` adds the directory-listing probe set), `audit` (offline over the crawl store, zero network), `status`, `sites` (publishes the whole rule table), `detect`, `hello`; `--fail-on {error,warning,suggestion,info}` is the pre-deploy gate hook | fallback by design — Detectify is SaaS and Burp Suite Pro is the paid GUI being replaced; shcheck is probed as the closest open checker and nuclei is surfaced but NEVER executed (it pulls its template pack over the network — the forbidden tier); the stdlib core is 100% of the product |
| 16 | Grafana Cloud / Tableau | deterministic static SVG chart renderer, 100% stdlib and no chart library: line / bar / scatter emitted by string template from a sqlite ledger (`--db X --table T`, opened `mode=ro` so charting `.scout/runtrack.db` or `.scout/uptime.db` can neither modify them nor contend for the write lock their daemons hold; table/column names are validated against `sqlite_master`/`PRAGMA table_info` before being quoted into the one interpolated SELECT), a CSV (`utf-8-sig`, so an Excel BOM does not corrupt the first column name), or a JSON array (`--records data.history` walks a dotted path, which makes this CLI's own `--json` envelope chartable without a reshaping step). **Byte-identical output for identical input** is the product, so a chart can live in git next to its data and show up in a diff when the numbers move: no generation timestamp anywhere, no reliance on source row order (a `SELECT` without `ORDER BY` is undefined, so points sort by (x, y), bar categories by `--sort label|value|-value`, series by label), no locale/TZ (UTC tick labels assembled from `time.gmtime` fields, never `strftime`), coordinates rounded to 2dp, and `newline="
"` on the write so Windows CRLF translation cannot fork the file — every command reports the SVG's `sha256` so the claim is checkable rather than promised. Honesty is enforced, not documented: a row whose y is not a number is SKIPPED and counted by reason (`skipped.bad_x/bad_y/missing`), never coerced to 0; bar duplicates are summed and the fold is stated in the footer; the bar y axis always reaches zero (a truncated bar axis misstates every ratio on it) while line/scatter do not; zero plottable rows renders an explicit empty panel with NO axis numbers instead of an invented range; and every chart carries a footer naming the source table/file, the row count, the columns and any `--where` predicate, so a picture of a subset can never pass as the whole table. The SVG is self-contained — inline CSS honouring `prefers-color-scheme`, zero JavaScript, zero external assets, `html.escape` everywhere, the only URL in the file being the SVG namespace itself — so opening a chart makes no request either | `scout charts` — `inspect`, `line`, `bar`, `scatter`, `kinds`, `detect`, `hello`; `inspect` is provenance-before-publication (same numbers, writes no file) and `--fail-on {error,warning}` is the cron/CI gate that fires when a panel goes empty (the classic dashboard failure: the graph still renders, nobody notices the feed died) | fallback by design — Tableau ships no CLI at all and Grafana ships a SERVER, so no native binary is a superset of a deterministic file; gnuplot and grafana are probed and surfaced but NEVER executed, because a spawned plotter would put the SVG bytes at the mercy of PATH contents and someone else's version string, destroying the diffability that is this adapter's entire reason to exist (the links #4 doctrine: output that changes with PATH is flaky by construction); the manifest disables the network axis entirely — nothing to fetch, no SaaS to feed — so "no datapoint left the box" is architectural; the stdlib renderer is 100% of the product |
| 28 | Copyscape | local near-duplicate detector, 100% stdlib: the corpus is the local tree and the SaaS upload is deleted (Copyscape's model is shipping the page you have not published yet to someone else's index, per query), so the manifest disables the network axis entirely AND writes too — `detect` proves it by running check_permission against copyscape.com and printing the denial. Ingestion sniffs the encoding by REUSING the logs #14 BOM/NUL sniffer (a UTF-16 draft decoded as UTF-8 is mojibake, and mojibake shingles match nothing — a silent "no duplicates"), tokenization REUSES prose #1's markdown/HTML strippers (code fences, inline code, URLs and tags blanked; nav/footer chrome is a stated limit, with extract #11's article bodies as the documented fix), and DOC_EXTS IS prose.PROSE_EXTS so the extension lists cannot drift. Broder shingling: k-token windows -> blake2b digests over tokens joined by a byte that cannot occur inside a token (so `ab|c` and `a|bc` differ), hashlib and never builtin hash() whose seed is randomized per process; Jaccard for "same page" PLUS containment |A n B|/min(|A|,|B|) for the partial lift a short draft into a long page that Jaccard structurally underweights; normalized-content sha256 catches recycled copy that differs only in wrapping, case or CRLF (what fdupes cannot see); union-find clusters so five variants report as ONE cluster of five. The hot path never intersects a set — one pass over the shingle->documents postings index yields the exact shared COUNT for every pair that shares a shingle and the rest are never visited, with `jaccard()` (definition) and `jaccard_from_counts()` (hot path) asserted against each other. Deterministic by contract: posix-relative ids, first-appearance shingle order, pairs ordered (kind, -similarity, a, b), clusters (-size, first member) — the same report on Windows and Linux, diffing clean when nothing moved. Honesty is enforced: every reading has EITHER a value OR a labelled reason (too-short names its token count and bound, no-tokens means no content hash because sha256("") is not evidence, an exact-content pair with no shingle sets reports similarity `null` with the reason, and binary/too-large/unreadable files are surfaced as info diagnostics) — never a fabricated 0.0 and never a silent skip | `scout dupes` — `scan`, `compare`, `config`, `detect`, `hello`; `scan --fail-on {error,warning,suggestion,info}` is the pre-publish gate hook (exact -> error, near -> warning, partial -> suggestion, unmeasured -> info, all four remappable as config) | fallback by design — Copyscape is SaaS; fdupes/rdfind/jdupes are probed and surfaced as BYTE-identical file finders (they cannot see a reworded paragraph) and jscpd as a node code-clone detector, none of them executed; the manifest denies network AND filesystem writes, so "the unpublished draft never left the box" is architectural; the stdlib shingling core is 100% of the product |
| 27 | Zapier / Make / n8n Cloud | local bounded workflow runner, 100% stdlib: the workflow is a JSON graph in your repo (`trigger -> transform -> action`) and the hosted runner is deleted — the manifest disables the network axis entirely AND there is deliberately no http/webhook action in the catalog, so "no payload ever left the box" is architectural rather than audited (Zapier's pricing unit is the *task*, i.e. the step, so free steps are half the swap). The execution model is the other half, because an automation runner that can loop forever or call anything it likes is a footgun with a cron entry: a HARD step cap on a budget SHARED with sub-flows (nesting buys no extra steps), cycles refused by validation AND a per-node visit cap at runtime (two independent guards — the test smuggles a cyclic graph past validate to prove the second one terminates it), sub-flow recursion refused BY NAME at a depth cap, and DEFAULT-DENY actions: one runs only when `--allow` names it, it is in the catalog, and this surface injected an effector for it, and any refusal STOPS the run (continuing past a skipped effect would run downstream nodes on a payload that never got it). Honesty is enforced, not documented: every step records EITHER an output OR an error (`_step` raises otherwise, and the ledger holds the same invariant), a transform whose source field is absent RAISES instead of rendering empty, a comparison against an absent field is False instead of a coerced 0, an effector that returns nothing is an error, a trigger that does not fire reports WHY, and a `schedule` trigger reads its last run out of the ledger instead of guessing a cadence. File actions are confined by `resolve_output_path` (absolute, drive-qualified and `..` paths refused, then structurally confirmed inside `--out-dir`) and write utf-8 BYTES so a Windows run and a Linux run produce identical files; run/step history lives in its own sqlite file (`.scout/flows.db`, never the #2 ledger's write lock) — Zapier's task pane as a local table | `scout flows` — `actions` (publishes the whole vocabulary plus a runnable example), `plan` (validate + preflight: what would run, what would be refused, executes nothing), `run`, `runs`, `detect`, `hello`; `plan --fail-on error` is the pre-schedule gate and `run --fail-on error` the cron/CI gate that fires on a refusal instead of quietly doing nothing | fallback by design — Zapier and Make are SaaS runners, n8n ships as a server, and flowpipe (probed as `native`, verified ABSENT on PATH 2026-07-24) runs its own HCL pipelines with none of these caps, so no local binary runs THIS graph under THESE bounds; the zapier CLI is surfaced but NEVER executed — its whole job is talking to the paid platform (the forbidden network tier); the stdlib runner is 100% of the product |
| 25 | Siteimprove | static accessibility checker over LOCAL html, 100% stdlib and no external parser: one tolerant `html.parser` pass extracts images, headings, form controls, labels, landmarks, ids and styled text runs, and the WCAG 2.x arithmetic (sRGB relative luminance using the 0.03928 threshold the normative text actually prints -> ratio -> AA/AAA thresholds with the 24px / 18.66px-bold large-text rule) runs over colors resolved by a deliberately tiny CSS resolver: inline `style` attributes plus the `html`/`:root`/`body` rules of `<style>` blocks, TWO passes so a trailing stylesheet still colours the markup above it, `em`/`rem`/`%`/`pt`/keyword sizes resolved down the inheritance chain, and UA defaults for h1-h6/small/b/strong/th so an unstyled `<h1>` is correctly judged at the LARGE threshold (every reading carries `font_source`, so a threshold resting on a UA default is visible rather than implied). 16 WCAG rules, each carrying its success criterion, plus one for a file that could not be read: alt presence (honouring `role=presentation`, `aria-hidden` and decorative `alt=""`, and flagging filename/generic alt text), label/control association (label[for], wrapping label, aria-label, aria-labelledby with a real id-existence check, title, button text, UA-labelled submit/reset), heading order and empty headings, landmark presence, duplicate ids, html lang, contrast at AA and AAA. Deliberately NOT a second copy of seo #3, which nominates a11y as an extension point: the input axis differs (local files with the network axis OFF vs a crawl frontier), single-h1 and page `<title>` stay SEO rules because WCAG 1.3.1 asks about heading ORDER, and `HTML_EXTS` is derived from `seo.HTML_EXTS` rather than retyped. Honesty is enforced, not documented: a contrast reading has EITHER a `ratio` OR an `error`, never both and never neither, so text coloured by a class, an `@media` block, a linked stylesheet, an `rgba()` alpha or a background image is reported as `a11y:contrast-unknown` WITH THE REASON instead of measured against an assumed white backdrop; the displayed ratio is rounded to 2dp but the verdict is computed unrounded, so 4.4989:1 shows as 4.5 and still FAILS AA; hidden subtrees (`hidden`, `aria-hidden`, `display:none`, `visibility:hidden`) are skipped and COUNTED so "0 findings" can never be confused with "nothing was looked at"; and an unreadable file reports `counts: null` plus an `a11y:file-unreadable` error rather than a row of zeros | `scout a11y` — `check`, `contrast`, `rules`, `detect`, `hello`; `check --fail-on error` is the pre-publish CI gate, `contrast --fg X --bg Y --fail-below AA` is the one-pair design check (an UNKNOWN reading fails that gate too), and `rules --rules org.json` is the policy-as-config overlay where an unknown rule id or a bad severity is a hard error instead of a silently ignored typo | fallback by design — Siteimprove is SaaS and every open checker in the category (axe-core CLI, pa11y) drives a headless browser that fetches the page over the network, so they are probed and surfaced but NEVER executed: running one would reintroduce the exact egress this adapter deletes and would make the verdict vary with PATH contents and someone's Chrome version (the links #4 doctrine); tidy's `-access` checks are surfaced for the same reason and have no contrast support at all. `detect` reports `native_used: false` on EVERY tier, so tier=native can never imply a binary produced the findings. The manifest disables the network axis with an EMPTY domain list and declares `filesystem.write: false` (this adapter writes nothing at all), and both `check` and `detect` call an `_egress_guard` that re-reads the manifest and REFUSES to run if that section was ever widened — so "no page ever left the box" is enforced rather than promised; the stdlib core is 100% of the product |
