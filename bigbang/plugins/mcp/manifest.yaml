name: mcp
version: 0.5.0
description: MCP client + server + meta — consume any MCP, serve bb as MCP, aggregate namespaces of downstream servers MetaMCP-style
capabilities:
  network:
    enabled: true
    domains: ["localhost", "127.0.0.1"]
  filesystem:
    write: true
    paths: ["~/.local/share/bigbang/"]
