name: system
version: 0.7.0
description: System health, audit, policy, scaffold
capabilities:
  network:
    enabled: true
    domains: ["localhost", "127.0.0.1"]
  filesystem:
    write: true
    paths:
      - "~/.local/share/bigbang/"
      - "bigbang/plugins/"
  secrets:
    allow: []
