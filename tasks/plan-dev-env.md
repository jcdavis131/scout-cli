# Plan — Dev environment optimize (standalone scout-cli)

Mirrors dottie cloud bootstrap: `scripts/dev-env-setup.sh` + `.cursor/environment.json` + `AGENTS.md`.
Primary workspace remains the dottie monorepo; this repo stays usable alone.
